[package]
name = "rust-utils"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = "0.12"
governor = "0.7"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
